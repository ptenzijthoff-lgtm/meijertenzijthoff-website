---
layout: layouts/base.njk
title: Columns
description: Columns van Meijer ten Zijthoff.
permalink: /columns/
---

<div class="page-intro">
    <h2>Columns</h2>
    <p>Korte, persoonlijke stukken over het dagelijks leven, lokale politiek en maatschappij.</p>
</div>

<div class="articles-list">
    {% for artikel in collections.columns %}
    <article class="article-card">
        <div class="article-meta">
            <span class="category column">Column</span> · {{ artikel.data.datum | dutchDate }}
        </div>
        <h3 class="article-title">
            <a href="{{ artikel.url }}">{{ artikel.data.titel }}</a>
        </h3>
        <p class="article-excerpt">{{ artikel.data.beschrijving }}</p>
    </article>
    {% else %}
    <p class="no-articles">Nog geen columns gepubliceerd.</p>
    {% endfor %}
</div>
